<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StockFlow">
        <activity
            android:name=".ui.launch.LaunchActivity"
            android:exported="true"
            android:theme="@style/Theme.StockFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.getstarted.GetStartedActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".ui.signup.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".ui.inventory.AddProductActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".ui.suppliers.PurchaseOrderActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".ui.dashboard.LowStockActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
        <activity
            android:name=".ui.dashboard.ReportsActivity"
            android:exported="false"
            android:theme="@style/Theme.StockFlow" />
    </application>

</manifest>
